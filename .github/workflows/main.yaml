name: Main
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  vendorito:
    runs-on: ubuntu-latest
    steps:
      - uses: Neosperience/vendorito@b6e7ba76be8528dbfbcc640ce79a4a682e35985b
        with:
          source: minio/minio
          target: quay.io/hamcha/minio-mirror
          credentials: ${{ secrets.QUAYCREDENTIALS }}
